export function P(props) {
    return(
    <p style={{fontSize:props.Size}} style={{color:props.color}} style={{textAlign:props.align}} style={{fontWeight:props.weight}}>{props.title}</p> 
        )
}